# AI Review Pulsator — dependencies
# Python 3.11+

langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0
typer>=0.12.0
pyyaml>=6.0
google-play-scraper>=1.2.7,<2
langdetect>=1.0.9
python-dotenv>=1.0.0

# LLM providers — dual split (see config.yaml langchain.classify / generate)
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0

# Dev / tests
pytest>=8.0
httpx>=0.27.0
